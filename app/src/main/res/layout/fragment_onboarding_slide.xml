<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="@dimen/spacing_xl">

    <!-- Slide Image -->
    <ImageView
        android:id="@+id/ivSlideImage"
        android:layout_width="@dimen/onboarding_image_size"
        android:layout_height="@dimen/onboarding_image_size"
        android:layout_marginBottom="@dimen/spacing_xl"
        android:contentDescription="Onboarding illustration"
        android:scaleType="centerCrop" />

    <!-- Slide Title -->
    <TextView
        android:id="@+id/tvSlideTitle"
        style="@style/Text.Title.Large"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/spacing_lg"
        android:textColor="@color/text_white"
        android:gravity="center"
        android:text="Slide Title" />

    <!-- Slide Subtitle -->
    <TextView
        android:id="@+id/tvSlideSubtitle"
        style="@style/Text.Subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/text_white"
        android:gravity="center"
        android:lineSpacingMultiplier="1.4"
        android:text="Slide subtitle text goes here with detailed description of the feature" />

</LinearLayout>